server:
  port: 8082

spring:
  application:
    name: employee-service

  data:
    mongodb:
      uri: mongodb+srv://directormaqs:ImGrateful1@cluster1.owfpvek.mongodb.net/employeeDb?retryWrites=true&w=majority

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
